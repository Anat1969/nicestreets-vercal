# הרחובות הטובים של אשדוד — Good Streets Ashdod

אפליקציית שיתוף ציבור (PWA) בעברית, mobile‑first, לאגף אדריכלות העיר בעיריית אשדוד
(Ashdod, Israel). תושבים לומדים מה הופך רחוב לטוב, בוחרים רחוב, מדרגים אותו בשבע שאלות,
מנמקים, מעלים תמונה — ועוקבים אחרי הסטטוס העירוני שהעירייה מעדכנת בגלוי.

המימוש נגזר מהאפיון שבמסמך "הרחובות הטובים של אשדוד — אפיון ופרומפט לבנייה".

## הרצה מקומית

```bash
npm install
cp .env.example .env.local     # קבעו STAFF_CODE
npm run dev                    # http://localhost:3000
```

בלי משתני Supabase האפליקציה עובדת מלאה מול מאגר JSON מקומי בתיקייה `.data/`
(מתעלמים ממנה ב‑git). זו ברירת המחדל לפיתוח ולהדגמה.

### נתוני הדגמה

היכנסו ל‑`/admin` עם ה‑STAFF_CODE ולחצו "הוספת נתוני הדגמה". הנתונים מסומנים
`is_demo` בנפרד מקולות אמיתיים, וניתן למחוק אותם בלחיצה אחת לפני פתיחה לציבור.

## פריסה לכתובת ציבורית (Vercel)

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2FAnat1969%2FNiceStreets-GH&env=STAFF_CODE&envDescription=%D7%A7%D7%95%D7%93%20%D7%94%D7%9B%D7%A0%D7%99%D7%A1%D7%94%20%D7%9C%D7%9C%D7%95%D7%97%20%D7%94%D7%91%D7%A7%D7%A8%D7%94%20%D7%A9%D7%9C%20%D7%94%D7%A6%D7%95%D7%95%D7%AA&project-name=good-streets-ashdod&repository-name=NiceStreets-GH)

לחיצה על הכפתור פותחת ייבוא של המאגר ב-Vercel. בשדה `STAFF_CODE` קבעי סיסמה לכניסת
הצוות ל-`/admin`, ולחצי Deploy. תוך כדקה מתקבלת כתובת ציבורית מסוג
`https://good-streets-ashdod.vercel.app`.

**שימי לב לאחסון בפריסה הראשונה:** בלי משתני Supabase האפליקציה משתמשת במאגר
JSON מקומי. ב-Vercel מערכת הקבצים אינה קבועה, ולכן קולות ותמונות עלולים להימחק
בכל פריסה או בהפעלה מחדש של הפונקציה. זה מספיק להדגמה ולסבב משוב פנימי; לפני
פתיחה לציבור יש לחבר Supabase:

1. ליצור פרויקט Supabase ולהריץ את `supabase/schema.sql`.
2. להוסיף ב-Vercel את משתני הסביבה `NEXT_PUBLIC_SUPABASE_URL`,
   `NEXT_PUBLIC_SUPABASE_ANON_KEY` ו-`SUPABASE_SERVICE_ROLE_KEY`.
3. לפרוס מחדש. האפליקציה עוברת אוטומטית לאחסון Supabase.

## מסכים

| מסך | נתיב | מה יש בו |
|---|---|---|
| בית | `/` | שאלה פותחת, שלושה עקרונות, מונים חיים, חמשת המובילים, סיכום סטטוסים |
| לבחור | `/choose` | שלושה שלבים: רחוב ורובע וסוג ← שבע שאלות 1–5 ← נימוק ותמונה |
| מפה | `/map` | MapLibre: מפת חום לפי רובעים ולפי קטעי רחוב, בחירת רובע מציגה את רחובותיו |
| רחובות | `/streets` | טבלת רחובות/רובעים עם מיון וסינון, וייצוא CSV ו‑GeoJSON לצוות |
| ללמוד | `/learn` | 12 קריטריונים בארבע משפחות, שש טיפולוגיות, ספריית דוגמאות |
| כרטיס רחוב | `/street/[id]` | קולות, ציון, פילוח לפי שאלות, גלריה, נימוקים, סטטוס עירוני |
| לוח בקרה | `/admin` | אישור תמונות, עדכון סטטוסים, רחובות לאימות, ייצוא, ניהול נתוני הדגמה |

## ארכיטקטורה

- **ממשק:** Next.js 15 (App Router) + TypeScript + Tailwind 4, RTL מלא, PWA.
- **מפה:** MapLibre GL. ברירת המחדל היא סגנון ריק בלי שרת אריחים חיצוני; להוספת
  מפת רקע עירונית הגדירו `NEXT_PUBLIC_MAP_STYLE` לכתובת style.json.
- **נתונים:** שכבת אחסון מופשטת ב‑`lib/store/`. שני מימושים לאותו ממשק:
  - `LocalStore` — קובץ JSON ב‑`.data/`, לפיתוח ולהדגמה.
  - `SupabaseStore` — Postgres + Storage, נכנס לפעולה כשמוגדרים
    `NEXT_PUBLIC_SUPABASE_URL` ו‑`SUPABASE_SERVICE_ROLE_KEY`.
- **הרשאות:** הדפדפן אינו פונה ל‑Supabase ישירות; כל בקשה עוברת דרך route handler
  ב‑Next.js שמאמת הרשאה. ה‑RLS ב‑`supabase/schema.sql` הוא קו הגנה שני.
- **קונפיגורציית עיר:** הכול מרוכז ב‑`lib/city.ts` — רובעים, רשימת רחובות,
  טיפולוגיות, קריטריונים, סטטוסים, צבעים וטקסטים. עיר אחרת מחליפה את הקובץ הזה בלבד.

### מודל הנתונים

`quarters`, `streets`, `votes`, `photos`, `street_status` — בדיוק כפי שהוגדר באפיון.
קול אחד לכל משתמש לכל רחוב (`unique (user_id, street_id)`); הצבעה חוזרת מעדכנת את
הקול הקודם ואינה מוסיפה קול חדש.

### פרטיות

- אין שמירת שם, טלפון או דוא"ל. המשתמש מזוהה במזהה אנונימי בעוגייה בלבד.
- התמונה מוקטנת ל‑1200px ומקודדת מחדש ל‑JPEG בדפדפן. הקידוד מחדש מוחק את כל שדות
  ה‑EXIF, כולל מיקום GPS.
- תמונה מתפרסמת רק אחרי אישור ידני של הצוות; עד אז היא גלויה לצוות בלבד.

## Supabase

```bash
psql "$DATABASE_URL" -f supabase/schema.sql
```

הסכמה כוללת PostGIS, אילוצים, RLS לכל הטבלאות ומדיניות אחסון לתמונות. חברי צוות
נוספים לטבלת `staff` דרך ה‑service role או מלוח הבקרה של Supabase; אי אפשר להוסיף
את עצמך דרך ה‑API.

## אימות נתוני GIS — לפני עלייה לאוויר

הגבולות של הרובעים, המרכזים שלהם ורשימת הרחובות שב‑`lib/city.ts` הם **נתוני
הדגמה סכמטיים**, ומסומנים ככאלה גם בקוד וגם בממשק. לפני פתיחה לציבור יש להחליף
אותם בשכבות ה‑GIS העירוניות:

```bash
npm run import:gis -- quarters.geojson streets.geojson > city-data.json
```

הסקריפט (`scripts/import-gis.mjs`) ממיר FeatureCollection לצורה ש‑`lib/city.ts`
מצפה לה, מסמן כל רחוב מיובא כ‑`verified: true`, ומדווח על רובעים או טיפולוגיות
שאינם מזוהים. שמות השדות הצפויים מתועדים בראש הסקריפט וניתנים להתאמה.

נושאים נוספים לאימות לפני עלייה לאוויר: מדיניות פרטיות ומדיניות תמונות (פנים
ולוחיות רישוי), ומי בצוות מעדכן סטטוסים ובאיזו תדירות. כמו כן, ניסוח 12
הקריטריונים כאן מבוסס על עקרונות מחקר "רחובות טובים" של מינהל התכנון ויש לאמת
אותו מול המסמך הרשמי.

## זיהוי

- **תושב/ת:** מזהה אנונימי בעוגייה, נוצר אוטומטית בהצבעה הראשונה.
- **צוות:** קוד צוות (`STAFF_CODE`) בעמוד `/admin`. מספיק ל‑MVP; לייצור מומלץ
  לעבור ל‑Supabase Auth עם קוד SMS ולטבלת `staff`, כפי שהסכמה כבר תומכת.

## נגישות

עברית ו‑RTL מלאים, ניגודיות לפי WCAG AA, גדלי מגע של 44px לפחות, ניווט מקלדת עם
קישור דילוג לתוכן, `aria-current` בניווט, `aria-sort` בטבלאות ו‑`role="status"`
להודעות מערכת.

## בדיקות שבוצעו

`npm run build` ו‑`npm run typecheck` עוברים. זרימת ההצבעה המלאה, העלאת תמונה,
אישור/דחייה, עדכון סטטוס, הייצוא והרשאות הצוות נבדקו מקצה לקצה מול השרת הבנוי
(כולל דפדפן אמיתי). אין עדיין בדיקות אוטומטיות בריפו.
