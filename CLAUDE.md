# הנחיות עבודה — הרחובות הטובים של אשדוד

מסמך זה נכתב אחרי סבב תקלות בפריסה הראשונה. מטרתו שהתקלות האלה לא יחזרו,
ושחלוקת העבודה תישמר: **ענת על התוכן, Claude על כל הצד הטכני.**

## חלוקת האחריות

- ענת מביאה תוכן והחלטות: קריטריונים, ניסוחים, רשימות רחובות, מדיניות.
- Claude אחראי על הכול מסביב: קוד, מסד נתונים, פריסה, חיבורים ואבחון תקלות.
- פנייה לענת נעשית רק כשאין דרך אחרת, ואז לפי הכללים שבסוף המסמך.

## ארכיטקטורה בקצרה

- Next.js 15 (App Router) + TypeScript + Tailwind 4, עברית RTL, PWA.
- אחסון מאחורי ממשק אחד ב-`lib/store/`: `SupabaseStore` בייצור,
  `LocalStore` (קובץ JSON) לפיתוח בלבד.
- כל הקונפיגורציה של העיר ב-`lib/city.ts` בלבד.
- `/api/health` הוא כלי האבחון הראשון בכל תקלה.

## שני מאגרי GitHub — לדחוף לשניהם

| מאגר | תפקיד |
|---|---|
| `Anat1969/NiceStreets-GH` | המאגר הראשי |
| `Anat1969/nicestreets-vercal` | **זה המאגר ש-Vercel פורסת ממנו** |

כפתור Deploy של Vercel יוצר מאגר חדש משלו. דחיפה למאגר הראשי בלבד לא
מגיעה לאוויר. **כל שינוי נדחף לשני המאגרים.**

## מסד הנתונים

פרויקט Supabase בשם `ASHDODMAP`, ref `cbhexpybdggwzyakgagd`. הסכמה המלאה
ב-`supabase/schema.sql`. תמונות נשמרות בטבלה `photo_blobs` כ-base64, כך
שכל מסד הנתונים הוא יחידה אחת לגיבוי.

## לקחים — כללים מחייבים

### 1. אף תקלה נקודתית לא מפילה את כל האתר
כל קריאה חיצונית עטופה בטיפול בשגיאות, כולל **יצירת הלקוח עצמו** ולא רק
השימוש בו. ערך שגוי במשתנה סביבה הפיל בעבר כל עמוד באתר, כולל עמוד
האבחון. כשמשהו נכשל — המסך נטען, מציג הודעה ברורה, וממשיך.

### 2. אבחון לפני בקשת עזרה
`/api/health` מדווח מה מוגדר, מה עובד ומה השגיאה המדויקת. הוא לעולם אינו
זורק שגיאה. לפני שמבקשים מענת צילום מסך — בודקים שם.

### 3. לעולם לא למסור ערכים ארוכים להעתקה ידנית דרך הצ'אט
מפתחות שמוצגים בצ'אט מוסתרים בנקודות (●), והעתקה מהם מייצרת ערך שנראה
תקין באורכו אך שבור. ערך שצריך להגיע למקום שאין לו גישה — מפנים למקור
המקורי שבו יש כפתור העתקה, לא מדביקים בצ'אט.

### 4. שם משתנה וערך — בשדות נפרדים
כשמנחים למלא משתנה סביבה, מפרידים במפורש בין ה-Key ל-Value. כתיבת שניהם
באותה מסגרת גרמה להדבקה של שניהם לתוך שדה הערך.

### 5. ערכים שאינם סודיים מקבלים ברירת מחדל בקוד
כתובת הפרויקט אינה סוד. כל ערך כזה נקבע בקוד כדי לצמצם את מספר השדות
שענת צריכה למלא. רק סודות אמיתיים נשארים כמשתני סביבה.

### 6. ניקוי ואימות של כל משתנה סביבה
רווחים, מרכאות, ירידות שורה ותווים שאינם ASCII — מנוקים או נדחים עם
הודעה בעברית שאומרת מה בדיוק לא תקין.

### 7. לאמת מול המקור, לא מול ההנחה
בדיקה מול מסד הנתונים דרך הכלי הישיר אינה מוכיחה שהאפליקציה עובדת מולו.
כשלא ניתן לבדוק מסלול מסוים — אומרים זאת במפורש במקום להניח שהוא תקין.

## כשצריך את ענת — מבנה ההסבר

פנייה אליה כוללת תמיד, בסדר הזה:

1. **מה הבעיה** במשפט אחד, בלי מונחים טכניים.
2. **איפה** — שם השירות, ואיך מגיעים למסך המדויק (קישור ישיר אם אפשר).
3. **מה לעשות** — צעדים ממוספרים, כל ערך בנפרד ומוגדר.
4. **למה זה עובד ככה** — משפט או שניים על ההיגיון, כדי שההבנה תישאר.
5. **איך יודעים שהצליח** — סימן ספציפי לבדיקה.

לעולם לא להפנות ל-`/admin` או לכל נתיב אחר בלי להסביר שמדובר בהוספת
טקסט לכתובת בדפדפן.
